<tr>
  <td><%= comment.text %></td>
  <td><%= link_to 'Edit', edit_article_comment_path(comment.article, comment) %></td>
  <td><%= link_to 'Destroy', article_comment_path(comment.article, comment), method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
